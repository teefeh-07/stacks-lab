import { Clarinet, Tx, Chain, Account, types } from "https://deno.land/x/clarinet@v1.0.0/index.ts";

Clarinet.test({
  name: "can register project",
  fn(chain: Chain, accounts: Map<string, Account>) {
    // Test logic
  },
});

Clarinet.test({
  name: "tokens allocated correctly",
  fn(chain: Chain, accounts: Map<string, Account>) {
    // Test logic
  },
});

